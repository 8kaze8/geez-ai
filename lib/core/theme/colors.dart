import 'package:flutter/material.dart';

class GeezColors {
  GeezColors._();

  // Primary
  static const primary = Color(0xFF1A73E8);
  static const primaryDeep = Color(0xFF1565C0);
  static const primaryLight = Color(0xFF4DA3FF);
  static const secondary = Color(0xFFFF6D00);
  static const accent = Color(0xFF00C853);

  // Surfaces
  static const surface = Color(0xFFFFFFFF);
  static const surfaceDark = Color(0xFF1E1E1E);
  static const background = Color(0xFFFAFAFA);
  static const backgroundDark = Color(0xFF121212);

  // Surface variants — for cards and containers that sit on top of background
  static const surfaceVariant = Color(0xFFF5F5F5);
  static const surfaceVariantDark = Color(0xFF2A2A2E);
  static const surfaceElevated = Color(0xFFF8F8F8);
  static const surfaceElevatedDark = Color(0xFF252528);

  // Text
  static const textPrimary = Color(0xFF212121);
  static const textPrimaryDark = Color(0xFFE0E0E0);
  static const textSecondary = Color(0xFF757575);
  static const textSecondaryDark = Color(0xFFB0B0B0);

  // Borders and dividers
  static const borderLight = Color(0xFFEEEEEE);
  static const borderDark = Color(0xFF3A3A3E);
  static const borderMutedLight = Color(0xFFE0E0E0);
  static const borderMutedDark = Color(0xFF424242);
  static const divider = Color(0xFFD0D0D0);
  static const dividerDark = Color(0xFF3A3A3E);

  // Map placeholder tones
  static const mapSkyLight = Color(0xFFD4E4F7);
  static const mapGroundLight = Color(0xFFE8EAF0);
  static const mapSkyDark = Color(0xFF1A2332);
  static const mapGroundDark = Color(0xFF2A3342);

  // Chat bubble backgrounds
  static const chatAiBubbleLight = Color(0xFFF0F4FA);
  static const chatAiBubbleDark = Color(0xFF2A2A2E);

  // Pending/inactive indicator
  static const pendingLight = Color(0xFFBDBDBD);
  static const pendingDark = Color(0xFF555555);

  // Warm amber (for legibility on light surfaces — e.g. email banner)
  static const warningDeep = Color(0xFFE65100);

  // Status
  static const error = Color(0xFFD32F2F);
  static const success = Color(0xFF388E3C);
  static const warning = Color(0xFFFFC107);

  // Persona categories
  static const foodie = Color(0xFFFF5722);
  static const history = Color(0xFF795548);
  static const adventure = Color(0xFF4CAF50);
  static const culture = Color(0xFF9C27B0);
  static const nature = Color(0xFF009688);
  static const discovery = Color(0xFFFFC107);

  // Destination card gradients
  static const gradientRomeStart = Color(0xFF795548);
  static const gradientRomeEnd = Color(0xFFD7CCC8);
  static const gradientAthensStart = Color(0xFF1565C0);
  static const gradientAthensEnd = Color(0xFF90CAF9);
  static const gradientBarcelonaStart = Color(0xFFE65100);
  static const gradientBarcelonaEnd = Color(0xFFFFCC80);

  // Progress bar background
  static const progressBg = Color(0xFFE3F2FD);
}
